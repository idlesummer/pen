// Placeholder - the real entry (import.meta.glob route discovery, router
// wiring, Ink render) lands in the next step. This file only exists so the
// Vite build has something real to bundle while builder.ts's own plumbing
// gets verified.
export {}
